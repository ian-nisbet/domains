parameters:
    # for "merge" command
    data_to_append:
        require-dev:
            phpunit/phpunit: '^6.0'
            symplify/monorepo-builder: '^7.3'

    # for "split" command
    directories_to_repositories:
        packages/gdpr: 'git@github.com/ian-nisbet//gdpr.git'
        packages/social-auth: 'git@github.com/ian-nisbet//social-auth.git'
